你是一位代码高手和科研专家，你将理解这几分数据.csv文件，并根据skills.md文件了解个科研任务。
1.你认为.md中还缺少哪些内容？你可以不断地询问和确认，用户给你答复。
2.给你答复后，你认为用户需要在.md文档处的何处完善哪些信息？以达到让ChatGPT能够看懂并根据每一个Phase的内容写出代码脚本的程度。

回答示例=======================
Answer1：
这份.md文档还存在xxxxx问题，缺少xxxxx信息，我将提问：

（在用户回答后，再进入Answer2）

Answer2：
根据你的回答，我将知道你如何修改.md文档以让ChatGPT能够看懂并根据每一个Phase的内容写出代码脚本：
修改位置1：
修改内容为：
……

========================================================================================
1.在我的框架中，每个5-15秒的instance会在Phase 0的prepare_bags.py中一次性提取并保存到bags.pkl里，feat_dim确定为522维，具体包括128维Mel频谱图（20Hz-150kHz范围）的mean、std、max、min四个统计值（共512维）加上10个时间域统计特征（RMS、ZCR、PeakAmp、Mean_IPI、TEKO均值、FDR均值、EnvStats均值/方差、TempCentroid等），这样Phase 1及后续所有Phase的训练和推理都可以直接加载预计算好的tensor[M, 522]，既避免实时提取开销，又完全适配RTX 4060的8GB显存和计算能力。
2.在我的框架中，一个“实例”（instance）严格定义为固定10秒的非重叠音频片段（默认值，可在prepare_bags.py中通过参数调整为5–15秒范围），每个bag30mins会按此长度均匀切分，例如一个180秒的bag会被切成18个实例，每个实例对应音频中连续的10秒时间窗口，并提取预计算的522维特征后保存为tensor[M, 522]，这样既能完整覆盖哨声或短脉冲串的时域结构，又避免了过短片段（如1–2秒）导致的上下文丢失，同时保持了MIL的bag-instance层级关系。
3.在我们的框架中，AttentionModule采用经典MIL注意力结构，W是一个可学习的权重矩阵（维度为[feature_dim, 128]），v是一个[128]维向量，通过v^T * tanh(W * feat + b)计算原始注意力分数后softmax归一化得到alphas；SimpleMLP特征提取器由3层全连接层组成，每层隐藏单元数固定为256并使用ReLU激活函数；MetadataEmbedder则是一个小型两层MLP，将水深、群大小、季节和行为编码后的4个标量特征映射到32维嵌入向量后与音频特征拼接。模型最终输出中，existence_pred[2]是通过一个共享的2单元线性分类头直接产生的两个独立logits（后续用sigmoid支持multi-label），而type_pred采用严格的分层设计，包含一个独立的6类哨声softmax头和一个独立的3类脉冲softmax头（总计9类概率，但两个头分开计算并用attention权重加权），从而实现哨声轮廓与脉冲子类的精确细分。
4.在我的框架中，数据分割严格按照录音文件（Ori_file_num）进行划分，而不是按bag划分，比例固定为70%训练集、15%验证集、15%测试集（例如前24个Ori_Recording用于训练，后5个用于验证，剩余6个用于测试），这样能确保同一段录音的所有bag只出现在同一个集合中，避免因同一录音内声学条件高度相关而导致的数据泄漏，同时保留了blocked cross-validation的特性以匹配真实部署场景。
5.在我的框架中，localization precision的量化方式是将每个高attention权重实例（10秒片段）的中心时间与真实标注时间（哨声的Whistle Begins或脉冲的Train_start）进行比较，如果绝对时间差在2秒以内则视为一次正确匹配，最终precision = 正确匹配数 / 总预测检测数；Phase 2的定位评估完全在时间戳级别进行，即模型通过attention权重计算出每个bag内最可能的具体秒数（实例开始时间 + 偏移），而非仅判断实例级别是否包含信号，从而实现毫秒级精度的弱监督定位。
6.在我的框架中，初始学习率固定为1e-4，优化器统一使用AdamW（weight decay=0.01，梯度裁剪1.0），批次大小固定为32（RTX 4060上稳定运行），训练轮数默认80轮并采用早停（patience=15），Focal Loss的gamma参数设为2.0（有效抑制易分类样本），alpha参数设为0.25（针对正例稀疏的whale/dolphin场景），所有这些值在src/train.py中直接硬编码并通过命令行参数可微调，但默认配置已在Phase 1-5的RTX 4060上验证过，能在6-10小时内完成全流程训练并达到稳定收敛。
7.采样率是576 kHz（576000 Hz），所有音频文件均为单声道（mono）。
=================================================================================================

根据这份回答，完善skill.md文档：
1.在我的框架中，每个5-15秒的instance会在Phase 0的prepare_bags.py中一次性提取并保存到bags.pkl里，feat_dim确定为522维，具体包括128维Mel频谱图（20Hz-150kHz范围）的mean、std、max、min四个统计值（共512维）加上10个时间域统计特征（RMS、ZCR、PeakAmp、Mean_IPI、TEKO均值、FDR均值、EnvStats均值/方差、TempCentroid等），这样Phase 1及后续所有Phase的训练和推理都可以直接加载预计算好的tensor[M, 522]，既避免实时提取开销，又完全适配RTX 4060的8GB显存和计算能力。
2.在我的框架中，一个“实例”（instance）严格定义为固定10秒的非重叠音频片段（默认值，可在prepare_bags.py中通过参数调整为5–15秒范围），每个bag30mins会按此长度均匀切分，例如一个180秒的bag会被切成18个实例，每个实例对应音频中连续的10秒时间窗口，并提取预计算的522维特征后保存为tensor[M, 522]，这样既能完整覆盖哨声或短脉冲串的时域结构，又避免了过短片段（如1–2秒）导致的上下文丢失，同时保持了MIL的bag-instance层级关系。
3.在我们的框架中，AttentionModule采用经典MIL注意力结构，W是一个可学习的权重矩阵（维度为[feature_dim, 128]），v是一个[128]维向量，通过v^T * tanh(W * feat + b)计算原始注意力分数后softmax归一化得到alphas；SimpleMLP特征提取器由3层全连接层组成，每层隐藏单元数固定为256并使用ReLU激活函数；MetadataEmbedder则是一个小型两层MLP，将水深、群大小、季节和行为编码后的4个标量特征映射到32维嵌入向量后与音频特征拼接。模型最终输出中，existence_pred[2]是通过一个共享的2单元线性分类头直接产生的两个独立logits（后续用sigmoid支持multi-label），而type_pred采用严格的分层设计，包含一个独立的6类哨声softmax头和一个独立的3类脉冲softmax头（总计9类概率，但两个头分开计算并用attention权重加权），从而实现哨声轮廓与脉冲子类的精确细分。
4.在我的框架中，数据分割严格按照录音文件（Ori_file_num）进行划分，而不是按bag划分，比例固定为70%训练集、15%验证集、15%测试集（例如前24个Ori_Recording用于训练，后5个用于验证，剩余6个用于测试），这样能确保同一段录音的所有bag只出现在同一个集合中，避免因同一录音内声学条件高度相关而导致的数据泄漏，同时保留了blocked cross-validation的特性以匹配真实部署场景。
5.在我的框架中，localization precision的量化方式是将每个高attention权重实例（10秒片段）的中心时间与真实标注时间（哨声的Whistle Begins或脉冲的Train_start）进行比较，如果绝对时间差在2秒以内则视为一次正确匹配，最终precision = 正确匹配数 / 总预测检测数；Phase 2的定位评估完全在时间戳级别进行，即模型通过attention权重计算出每个bag内最可能的具体秒数（实例开始时间 + 偏移），而非仅判断实例级别是否包含信号，从而实现毫秒级精度的弱监督定位。
6.在我的框架中，初始学习率固定为1e-4，优化器统一使用AdamW（weight decay=0.01，梯度裁剪1.0），批次大小固定为32（RTX 4060上稳定运行），训练轮数默认80轮并采用早停（patience=15），Focal Loss的gamma参数设为2.0（有效抑制易分类样本），alpha参数设为0.25（针对正例稀疏的whale/dolphin场景），所有这些值在src/train.py中直接硬编码并通过命令行参数可微调，但默认配置已在Phase 1-5的RTX 4060上验证过，能在6-10小时内完成全流程训练并达到稳定收敛。
7.采样率是576 kHz（576000 Hz），所有音频文件均为单声道（mono）。

同时，我要求数据要下采样为45kHz进行处理，防止计算复杂度过高。同时一些必要的、可能需要调整的配置可以放在config文件里。将这个要求也加载.md文件中