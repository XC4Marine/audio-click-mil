## 2026/03/28
### 主要内容：
#### 1.预实验：
采用简单注意力机制、简单平滑降维到32\128维度，50轮训练。研究teager\mfcc\logmel\cqt\gammaton特征在该任务中的效果。
```
脚本：scripts\feature_extraction.py、scripts\train_simple_mil.py

使用方式：python D:\Project_Github\audio_click_mil\scripts\feature_extraction.py --method mfcc（可替换为 teager/wavelet/logmel/cqt/gammatone）
python D:\Project_Github\audio_click_mil\scripts\simple_mil_train.py --feature_method mfcc

输出：
所有特征均生成一份-txt报告，bag level的-csv报告，instance level的-csv报告。
features（删除）、models。
（以上代码和文件最后保存在：实验记录\2026-03-28\预实验 中）
```
##### 实验记录：
| Accucacy | 32维 | 128维 |
|-------|-------|-------|
| Teager | 0.56 | 0.54 |
| Mfcc | 0.66 | 0.51 |
| logmel | 0.53 | \ | 
| cqt | \ | 0.53 |
| gammaton | \ | 0.56 |
| wavelet(1h起步,不做) | \ | \ |

##### 结论：
1.均值平滑很有可能会稀释Click信号；噪声（如低频隆隆声）虽然频率低，但总能量（均值）可能远高于极短促的 Click。如果特征只取 Mean，模型会认为能量大的噪声段更重要。

2.本应该适用于高频脉冲的CQT和Gammatone（？），在1s的平均值下，也无法分辨噪声和点击——instance变长or改变平均值策略。

3.gammatone、logmel、mfcc的注意力机制是有在工作的，但是值都非常小，错误关注噪声部分了。在水下声学中，Click 信号是典型的非平稳瞬态信号，而噪声（如背景涌浪、船舶机械声）通常是持续性或周期性的。如果模型错误关注噪声，通常是因为噪声的“统计特征”比 Click 更容易被线性层识别。

4.简单注意力机制使用softmax，在该任务中将不再适用。Click信号具有稀疏性，softmax会给每一个instance都至少分配一点权重；在 MIL（多实例学习）中，如果 60 个实例中有 59 个是噪声，Softmax 会把这 59 个噪声的微小权重累加起来。最终，噪声的总权重可能远超那唯一一个 Click 信号的权重，导致模型“看走眼”。

#### 2.进一步实验：

##### 任务1.画出某一个正包的不同特征图。
1. 修改scripts\03_instance_bag_npy.ipynb 代码。决定将instance按照每一个audio wave的最大值进行[-1,1]的归一化；同时完成每个instance level标签的生成，便于我观察正负示例的区别。

2. 正包采用了processed_data\balanced_bags\file_01_bag_006_label_1.npy,结果保存在："实验记录\2026-03-28\进一步实验\bag_visualization\reports".

3. 正例采用processed_data\balanced_bags\file_01_bag_006_label_1.npy的第1个实例，并标注出信号区域。负例采用其中的第二个实例，结果保存在"实验记录\2026-03-28\进一步实验\instance_visualization\reports"。

```
代码：
实验记录\2026-03-28\进一步实验\bag_visualization.ipynb
实验记录\2026-03-28\进一步实验\instance_visulization_negative.ipynb
实验记录\2026-03-28\进一步实验\instance_visulization_positive.ipynb
```
##### 结论：
1. 采用Teager算子和log-mel谱，特征明显。
2. 包级特征的生成，在不降采样的情况下存在困难，内存占用大。若降采样，则容易去除真正的click信号。侧面证明该任务需要采用：'切分成实例-MIL'的策略。
3. 正负例的峰值因子和峭度会存在比较大的差异。
4. 画图中的参数固定下来，作为后续模型的输入。
5. CQT和MFCC的效果要存疑，Wabelet提取出来的波形也难以找到click。Gammatone、log-mel的波形是差不多的。按理来说Teager会比Hilbert更适合Clikc信号的检测。

* 后续Teager要设定一个阈值，或者滤除掉不需要的噪声成分，或者转化成一个值？没必要处理200，000个点来生成一个权重。采用一些传统文献的做法，算出信噪比，高于噪声多少dB，就记为Click，可以就去算这几个Click的能量，带上位置信息————依然会存在误检，这是正常的，这一步是要去包括尽量多pulse信号，无论是噪声还是click。只不过，用机器学习代替人工筛查，然后人能更方便地直接定位到信号，进行后续分析。

## 2026/03/29

##### 任务2.引入 StepLR 学习率衰减。
加入了StepLR，每 20 轮自动降低学习率。使用logmel平滑特征，进行50轮的训练，准确率提升至0.79，注意力机制中的真实目标占比是0.2391。但是模型鲁棒性不足，不同随机种子带来的差异大，有时准确率只有0.58。说明在权重初始化的影响比较大。同时。如果某个种子初始化的 proj 层对噪声敏感，模型就会把注意力全部分配给噪声，从而学不到真正的 Click 特征。

改进：加入了线性warm-up机制，使用 LambdaLR 实现了前 10 个 Epoch 的线性预热，这能让模型在初期面对 1:59 的稀疏信号时更稳健。
结果：依然不好，在0.4-0.6之间，但是Loss很低，说明过拟合验证

改进：加入Dropout
结果：效果不佳，0.48

改进：加入weight Decay，增加 weight_decay (L2 正则化)，惩罚过大的权重
效果：效果不佳，0.61

改进：1. 瓶颈结构（Bottleneck）：先压缩再映射
你目前的 proj 层是将 128 维映射到 128 维。如果 Click 信号在 Log-Mel 中很稀疏，128 维的特征空间对于单秒信号来说太“宽”了，模型会把背景噪音也当成特征存进去。
建议修改： 尝试先降维，强迫模型进行特征压缩（去噪），然后再进行注意力加权。
2. 注意力池化层的隐藏维度
你现在的 self.attn 是一个简单的 Linear(128, 1)。这意味着注意力权重的计算是一次性的线性映射，缺乏非线性表达力。
如果不同种子的表现差异巨大，说明简单的线性注意力很不稳定。建议给注意力机制增加一个隐藏层：

模块,当前配置,建议配置,理由
proj 隐藏层,128,64 或 32,增加瓶颈效应，过滤背景噪音
Dropout,无 / 极低,0.3 - 0.5,强制模型不依赖特定神经元，防止“背诵”
Attention,线性 (128→1),两层 MLP (64→32→1),提高寻找 Click 信号的稳定性
BatchNorm,有,保留,必须保留，稳定特征分布

结果：效果不佳，且依然不稳定。接下来将聚焦使用的特征。

使用了门控机制
结果：效果不佳

用teager试了以下
结果：效果不佳
```
代码:实验记录\2026-03-29\任务2\train_simple_mil.py
不知道什么时候AI帮我把特征都换为了mean+max
```

##### 任务3.更新模块
|序号| 模块 | 修改 | 预期效果 |box|
|-------|-------|-------|-------|-------|
|1|特征工程改进：引入“统计量特征堆叠”|CQT等+[Mean, Max, Std, Kurtosis] 拼接|捕捉瞬态冲击，不被噪声稀释。Click 发生时，Max 会骤增，Mean 几乎不变。只有引入 Max，模型才能感知到脉冲的存在。|-|
|2|-------|[Min,Max]对|如果模型发现某个实例的 Max >> Mean，门控注意力会更容易捕捉到它。|-|
|-------|-------|希尔伯特变换 (Hilbert Transform)|通过希尔伯特变换提取信号的瞬时包络 (Instantaneous Envelope)。相比 Teager 算子，希尔伯特包络对噪声的鲁棒性稍好一些，能清晰地勾勒出 Click 的能量轮廓。|-------|
|3|-------|引入峭度 (Kurtosis)|背景噪声的峭度通常在 3 左右（接近正态分布）Click 信号的峭度通常会达到 10-50建议： 在 128 维中预留 8 维给每个子带的峭度。|-|
|-------|-------|时域包络特征(Temporal Envelope Features)|Click 信号最显著的特点是“突发性”。峰值因子 (Crest Factor)：信号峰值与均方根（RMS）的比值。Click 声的峰值因子极高。峭度 (Kurtosis)：衡量信号的“冲击性”。正常的背景噪声接近高斯分布（峭度≈3），而 Click 信号会让峭度飙升到几十甚至上百。过零率 (Zero Crossing Rate, ZCR)：Click 信号通常含有高频成分，其过零率会显著高于背景低频涌浪噪声。|-------|-------|
|-------|-------|谱质心与谱展开 (Spectral Centroid & Spread)|Click 信号在频域上通常不是平坦的。谱质心 (Spectral Centroid)：代表声音的“亮度”。Click 声的能量中心通常在几万甚至十几万赫兹（Hz）。谱平坦度 (Spectral Flatness)：Click 信号在短时间内更接近冲击脉冲，其频谱比纯音平坦，但比白噪声更有结构。|-------|-------|
|4|改进注意机制|使用类似于噪声滤除的方法|过滤掉全噪声的实例（权重趋于 0），只给含有疑似 Click 的实例高权重。|-|
|5|-------| 增加温度系数 (Temperature) | Softmax(xi​/T)，取 T<1 (如 0.1)  放大差异。让大的更大，小的更小，迫使权重向极值集中 |-|
|6|-------| **门控机制 (Gating)**|A=Softmax(V⋅σ(U)).过滤噪声。利用 σ (Sigmoid) 的 0-1 特性直接关掉噪声通道|-|
|7|-------|Sparsemax|替代 Softmax。真正置零，可以将不相关的权重直接强制设为 0|-|
|8|-------|Top-K Pooling|只取前 K 个最大权重的实例。只看重点。直接忽略掉贡献小的实例|-|
|9|改变输入粒度|1 秒音频 $\rightarrow$ 128 维向量（1D）。|让模型在实例内部使用 1D-CNN 或 Max-Pooling 自行寻找 Click。手动平滑降维丢失了太多时频信息。|-|


改进：特征全采用max
1.时间轴压缩的logmel - acc=0.63
2.频率轴压缩的logmel - acc=
3.teager 算子 - acc=0.5，但instance的定位性能也来到了0.5
